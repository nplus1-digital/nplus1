# this is only for search engines, it does not appear in the home
# as long as the json variables name and description are provided
title: N+1 Digital
description: Fractional CMO + Growth Tech for startups.
baseurl: ""
repository: https://github.com/nplus1-digital/nplus1

# this information is shown on the homepage, all fields are optional and
# can be declared either as a single string or as a string array/hash
json:
  name: N+1 Digital 
  description:
    - Fractional Growth
    - For Startups & Ecommerce Brands
    - Click below to start 👇
  start: book a meeting >
  contact:
    phone: +1 913-593-1084
    email: ryan@nplus1.digital
  source: 

exclude:
  - LICENSE
  - README.md

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

whitelist:
  - jekyll-sitemap

sass:
  sass_dir: _sass
  style: compressed
  sourcemap: never
